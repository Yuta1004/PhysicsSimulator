version: "3"
services:
    front:
        build:
            context: ./
            dockerfile: ./front/Dockerfile
        environment:
            TZ: Asia/Tokyo
        command: npm start
        ports:
            - $PORT:3000
